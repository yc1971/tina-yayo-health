{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node dist/index.cjs",
    "healthcheckPath": "/api/studies",
    "restartPolicyType": "ON_FAILURE"
  }
}
